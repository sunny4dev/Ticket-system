apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  NODE_ENV: production
  UPLOAD_DIR: "/app/uploads"
  DB_HOST: db-service
  DB_PORT: "5432"
  VITE_API_URL: "http://api.ticketing.local"
  CORS_ORIGINS: "http://ticketing.local,http://localhost:80,http://localhost:5173,http://localhost"